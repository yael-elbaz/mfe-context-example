# Private registry for the @org scope.
# Replace the placeholders with your npm artifact feed URL.
# The auth token comes from an env var (NPM_TOKEN) — never hardcode it here.
@org:registry=<<YOUR_PRIVATE_REGISTRY_URL>>
# //<<registry-host>>/:_authToken=${NPM_TOKEN}
